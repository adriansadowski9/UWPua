<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
             x:Class="UWP_UAapp.Views.NewItemPage"
		     Title="New Item">
    <ContentPage.ToolbarItems>
        <ToolbarItem Text="Save" Clicked="Save_Clicked" />
    </ContentPage.ToolbarItems>
    <ContentPage.Content>
        <StackLayout Spacing="20" Padding="15">
            <Label Text="Name" FontSize="Medium" />
            <Entry Text="{Binding Item.Text}" FontSize="Small" />
            <Label Text="Description" FontSize="Medium" />
            <Editor Text="{Binding Item.Description}" FontSize="Small" Margin="0" />
            <Label Text="Street" FontSize="Medium" />
            <Label Text="{Binding Item.Street}" FontSize="Small"/>
            <Label Text="Zip code" FontSize="Medium" />
            <Label Text="{Binding Item.Zip_Code}" FontSize="Small"/>
            <Label Text="City" FontSize="Medium" />
            <Label Text="{Binding Item.City}" FontSize="Small"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>
