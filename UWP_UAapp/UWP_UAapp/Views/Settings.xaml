<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="UWP_UAapp.Views.Settings"             
             Title="{Binding Title}"
             BackgroundColor="{DynamicResource backgroundColor}">
    <Grid>
        <!--<Grid.RowDefinitions>
            <RowDefinition Height="Auto" />           
        </Grid.RowDefinitions>-->
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Label Text="Dark Theme" Grid.Row="0" Grid.Column="0" Margin="5,15,0,0" XAlign="Center" YAlign="Start" TextColor ="{DynamicResource textColor}"/>
        <Switch Grid.Row="0" Grid.Column="2" Margin="0,15,5,0" IsToggled="{Binding DarkMode}" VerticalOptions="Start" Toggled="ToggleDarkMode"/>



    </Grid>

</ContentPage>